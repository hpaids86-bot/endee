Retrieval-Augmented Generation (RAG) is a technique that enhances generative AI models with facts fetched from external sources. The primary advantage of RAG is that it grounds the model on truth, reducing hallucinations. Sentence Transformers are lightweight models used to convert text into vector embeddings. A vector embedding is an array of numbers representing the semantic meaning of text. Cosine similarity is an algorithm used to measure how similar two vectors are.